---
import type { Translations } from '../i18n/es';

interface Props {
  t: Translations;
}

const { t } = Astro.props;
---

<div
  id="cookie-banner"
  class="fixed bottom-0 left-0 right-0 z-50 bg-navy-800 text-white px-4 py-4 shadow-lg border-t border-white/10"
  role="dialog"
  aria-live="polite"
  aria-label="Aviso de cookies"
>
  <div class="max-w-6xl mx-auto flex flex-col sm:flex-row items-center justify-between gap-4">
    <p class="text-sm text-blue-200 flex-1">
      {t.cookieBanner.message}{' '}
      <a href="#" class="text-gold-300 hover:text-gold-200 underline">{t.cookieBanner.privacy}</a>
    </p>
    <button
      type="button"
      id="cookie-accept"
      class="btn-primary flex-shrink-0 py-2 px-4 text-sm"
    >
      {t.cookieBanner.accept}
    </button>
  </div>
</div>

<script is:inline>
  (function () {
    const storageKey = 'bonae_cookies_accepted';
    const banner = document.getElementById('cookie-banner');
    const accept = document.getElementById('cookie-accept');
    if (!banner || !accept) return;

    if (typeof localStorage !== 'undefined' && localStorage.getItem(storageKey)) {
      banner.classList.add('hidden');
      return;
    }

    accept.addEventListener('click', () => {
      if (typeof localStorage !== 'undefined') localStorage.setItem(storageKey, 'true');
      banner.classList.add('hidden');
    });
  })();
</script>
