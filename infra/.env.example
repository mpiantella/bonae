# Optional: copy to .env. Then use npm scripts that load it:
#   npm run bootstrap:env
#   npm run deploy:env

# Target for this CDK app (must match bootstrapped account + Region)
CDK_DEFAULT_ACCOUNT=123456789012
CDK_DEFAULT_REGION=sa-east-1

# AWS auth (pick one approach)
# AWS_PROFILE=my-sso-profile
